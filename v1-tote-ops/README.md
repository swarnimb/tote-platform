# Tote-Ops

Personal operations dashboard for a solo IBC tote sourcing contractor. Manages suppliers, pickups, gradeouts, invoices, and leads. Single user. iPad primary.

## Setup

```bash
cp .env.example .env
# Fill in real values in .env
```

[Add install and run commands after stack is confirmed in @plan.]

## Environment Variables

See `.env.example` for required variables.

## Tests

[Add test command after stack is confirmed in @plan.]
